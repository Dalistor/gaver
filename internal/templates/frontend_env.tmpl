# Variáveis de ambiente para o Frontend (Quasar/Vite)
# Variáveis com prefixo VITE_ são expostas automaticamente pelo Vite
# Este arquivo é gerado automaticamente - edite o .env na raiz do projeto

VITE_API_URL=http://localhost:{{.ServerPort}}/api/v1
VITE_SERVER_PORT={{.ServerPort}}

