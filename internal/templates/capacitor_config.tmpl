import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.{{.ProjectName}}.app',
  appName: '{{.ProjectName}}',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
  plugins: {
    Filesystem: {
      android: {
        path: 'files'
      }
    }
  }
};