package {{.ModuleName}}

import (
	"{{.ProjectName}}/modules/{{.ModuleName}}/handlers"
	"{{.ProjectName}}/modules/{{.ModuleName}}/services"
	"{{.ProjectName}}/modules/{{.ModuleName}}/repositories"
	
	"github.com/gin-gonic/gin"
)

// Module representa o módulo {{.ModuleName}}
type Module struct {
	Name string
}

// NewModule cria uma nova instância do módulo
func NewModule() *Module {
	return &Module{
		Name: "{{.ModuleName}}",
	}
}

// RegisterRoutes registra as rotas do módulo
func (m *Module) RegisterRoutes(router *gin.RouterGroup) {
{{.RoutesCode}}
}

// Init inicializa o módulo (migrations, seeders, etc)
func (m *Module) Init() error {
	// Inicialização do módulo aqui
	return nil
}

